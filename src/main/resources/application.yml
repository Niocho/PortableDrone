server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active:
      - 'dev'
  jpa:
    open-in-view: true
    generate-ddl: true
    hibernate:
      ddl-auto: update